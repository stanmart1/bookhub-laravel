import React from 'react';
import { Button } from '@mui/material';

const TwoFactorSetup = () => {
  return <Button variant="contained">Enable Two-Factor Authentication</Button>;
};

export default TwoFactorSetup;
