import React from 'react';
import { Box, Typography } from '@mui/material';

const ReadingHeatmap = () => {
  return (
    <Box>
      <Typography variant="h6">Reading Heatmap</Typography>
      <Typography>
        This component will display a heatmap of reading activity.
      </Typography>
    </Box>
  );
};

export default ReadingHeatmap;
