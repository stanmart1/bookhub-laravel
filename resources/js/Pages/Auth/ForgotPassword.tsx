import React from 'react';
import ForgotPasswordForm from '../../Components/Auth/ForgotPasswordForm';

const ForgotPassword = () => {
  return <ForgotPasswordForm open={true} onClose={() => {}} />;
};

export default ForgotPassword;
